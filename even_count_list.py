#Counts the number of even numbers in a list

import sys 
def even_count(lst):
	linky = LinkedList(lst)
	ptr = self.head
	while ptr != None:
		if ptr[linky] % 2 == 0:
			count = count + 1
		ptr = ptr.next
		return count

